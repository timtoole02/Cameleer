# Cameleer — STATUS (canonical)

> **This file is the single source of truth for what Cameleer actually is.** It is
> re-derived from code and fresh runs, not copied from any prior `.md`. Every capability
> claim links a `file:line`, a test, or a receipt under
> [`control-center/receipts/`](control-center/receipts/). A prose claim with no linked
> artifact is a defect, not documentation.
>
> **Two lanes** (borrowed from Camelid):
> - **Supported** — wired end-to-end (backend logic + registered `#[tauri::command]` + a UI
>   surface) **and** covered by a test or an e2e receipt.
> - **Runnable** — code exists, compiles, and is reachable, but is not surfaced in the UI
>   and/or not tested. Explicitly labeled; never described as a shipping feature.
>
> Generated by HARDPAN G0 on branch `hardpan` (base `main` @ `8834015`, 2026-06-02).
> Companion: [COMMAND_MAP.md](COMMAND_MAP.md) (every command → registered/wrapper/UI/test).

---

## 0. What Cameleer is / is not

**Is:** a local-first macOS (Apple-Silicon) **Tauri 2** desktop app (`control-center/`) — a
React 19 + Zustand frontend over a Rust backend of ~36 modules and 146 compiled Tauri
commands, persisting to a durable SQLite DB at `~/.cameleer/cameleer_workspace.db`
(`control-center/src-tauri/src/storage.rs:9,18`). It vendors the **camelid** GGUF inference
engine (`camelid/`) as a bundled runtime binary.

**Is not (yet):** a verified release. As of G0 there is **no CI**, the release gate is
broken, the safety-critical execution core is untested, a legacy pre-pivot gateway crate is
still compiled into the workspace, and several docs describe UI that does not exist.

---

## 1. Build & environment reality (verified on the work machine)

Host: macOS 26.3 (arm64), `cargo 1.96.1`, `node v26.5.0`. Receipts:
[`receipts/frontend-baseline.check-receipt.json`](control-center/receipts/frontend-baseline.check-receipt.json).

| Check | Result | Evidence |
|---|---|---|
| Frontend `tsc --noEmit` | ✅ clean | local run, receipt |
| Frontend `vitest run` | ✅ 3/3 (1 file) | local run, receipt |
| Frontend `p0-smoke` | ✅ pass (static grep, **not runtime**) | local run, receipt |
| Backend `cargo check -p control-center` | ✅ compiles — **but only after the camelid resource is staged** | build script fails with `resource path 'camelid' doesn't exist` until `npm run prepare:camelid` builds+stages `src-tauri/camelid` |
| Backend `cargo test -p control-center` | ✅ **44 passed, 0 failed** (1 compiler warning) | [`backend-baseline.check-receipt.json`](control-center/receipts/backend-baseline.check-receipt.json) — README's "44/44" **confirmed**; AUDIT's "43" is wrong |
| camelid binary CLI | `serve` / `inspect` / `tensor-dump` / `bench-*` subcommands; **`--version` is rejected** | `camelid --help`. The mandate's proposed "verify `--version` runs" gate would FAIL against the real binary; `release-check.sh:44` only survives via `|| true`. |

---

## 2. The 15 mandate requirements — two-lane ledger

Re-derived from `lib.rs`, migrations, per-module source, `src/api/*`, `pages/*`, and test
bodies (not from `AUDIT_REAL_VS_FAKE.md`). **Supported = 9, Runnable = 6.**

| # | Requirement | Lane | Backend (file:line) | UI | Test evidence / gap |
|---|---|---|---|---|---|
| 1 | Agent registry | **Supported** | `agent_registry.rs:35,87,126,169` | AgentsPage | `e2e_persistence` asserts agents persist; `agent_contracts` seeded test |
| 2 | Nested org chart | **Supported** | `org_services.rs:105,140,271,312` | AgentsPage tree | `org_services sync_creates_one_org_node_per_agent…` + e2e |
| 3 | Chat workspace | **Supported** | `chat_service.rs:20,55,106,306` | ChatPage | 3 guard tests (offline/db-not-ready/model-missing). **Happy-path reply is model-gated — untested** |
| 4 | Task & Kanban | **Supported** | `task_manager.rs:817,966,1003`; `board_services.rs:920,985` | KanbanPage + TaskDetailDrawer | `storage.rs move_task_persists / assign… / complete_task_moves_to_done` + e2e |
| 5 | Backlog | **Supported*** | `board_services.rs:528,580,650,746` | BacklogPage | *only test is schema-shape (`backlog_schema_has_refinement_fields`) — no create/convert behavior test |
| 6 | Shared memory | **Supported** | `memory_engine.rs:21,33,76,95` | MemoryPage | `e2e_persistence.rs:111,176` save+search across restart |
| 7 | Project context | **Runnable** | `context_engine.rs:442,478,568` | ContextPage | `context_engine` has **zero tests**; e2e only persists `projects` rows |
| 8 | Model profiles | **Runnable** | `models_manager.rs:440,1090,1208,1310`; `router` save_provider_config | ModelsPage | **no** models_manager/router test; e2e checks `active_model_profile` via raw SQL only |
| 9 | Runtime execution state | **Supported** | `task_manager.rs:1108,3028,3080`; `agent_runtime_kernel.rs:15` | RuntimePage + AgentRunPanel | `storage.rs start_agent_task_creates_run` + progress-update test. **Live status is model-gated** |
| 10 | Tool execution records | **Runnable** | `agent_tool_controller.rs:15` (via `task_manager.rs:1381`) | approvals only | records written + tested (`collects_files_commands_and_tests…`) but **no command/UI surfaces raw invocations** |
| 11 | Audit logs | **Runnable** | `mission_builder.rs:865,1239`; `system_services.rs:51` | AuditPage → `get_mission_audit_events` | **UI reads the wrong table** — see §5. `get_sandbox_audit_logs` is **unregistered** |
| 12 | Persistence across restart | **Supported** | `storage.rs:22`; migrations 0001–0004 | all pages reload | `e2e_persistence workspace_state_survives_close_and_reopen` + idempotent-migrations |
| 13 | Error handling | **Supported** | `command_guard.rs:22`; `system_services.rs:433` | RecoveryPanel / ErrorState | chat guards, health schema-error test, crash-frequency test, state-machine invalid-transition tests |
| 14 | Settings | **Runnable** | `settings`/`autopilot_settings` tables; get/update commands | SettingsPage (read-only) | settings table tested, but **Settings UI never reads/writes via the settings commands**; `api/settings.ts` is dead code |
| 15 | macOS Tauri build | **Runnable** | `tauri.conf.json` (productName "Cameleer", id `com.cameleer.desktop`) | whole app | shipped `Cameleer_0.1.0_aarch64.dmg` proves it built once; **no automated build/smoke gate** |

### Definition of Done — walkthrough (mandate.txt:107)
The chain is mechanically reachable end-to-end, with **two soft links**:
- **"see runtime status"** depends on a live model backend — with no model, RuntimePage stays empty.
- **"inspect audit logs" is effectively broken** — AuditPage reads `mission_audit_events`,
  written only on mission-preview-apply (`mission_builder.rs:865`). Creating projects, moving
  cards, and storing memory write to `events`/`task_activity` and the sandbox log — **none
  surfaced on the Audit page.** A normal DoD walkthrough yields an empty Audit page.

The literal DoD sentence ("close app, reopen, same state") **is** tested:
`e2e_persistence::workspace_state_survives_close_and_reopen`.

---

## 3. Command surface (see [COMMAND_MAP.md](COMMAND_MAP.md))

| metric | value |
|---|---:|
| `#[tauri::command]` in source | 149 |
| …actually compiled | **146** (`skills_manager.rs` is **not declared as `mod`** in lib.rs → 3 dead commands) |
| Registered in `generate_handler!` (`lib.rs:89–229`) | **138** |
| Reach a UI surface | **48** |
| Registered but no wrapper & no UI (orphans) | **71** |
| Unregistered (uncallable from JS) | **11** |
| Frontend GHOST calls (command doesn't exist) | **0** |

The 71 orphans + 11 unregistered are **Runnable, not Supported**, regardless of what
`SPRINT_LOG.md` says. G5 will wire-or-label each.

---

## 4. Test coverage reality

- **12 of 36 backend modules** contain a `#[cfg(test)]` block; **24 have none.**
- The **safety-critical execution core is entirely untested**: `command_guard.rs` (the
  sandbox), `agent_tool_controller.rs` (spawns processes / writes files),
  `agent_runtime_kernel.rs`, `agent_validation_engine.rs`, `models_manager.rs`,
  `camelid_adapter.rs`. This is the whole point of G3.
- **Measured: 44 backend tests pass, 0 fail** on this host
  ([receipt](control-center/receipts/backend-baseline.check-receipt.json)). README's
  "44/44" is **confirmed**; `AUDIT_REAL_VS_FAKE.md`'s "43" is wrong. But those 44 tests span
  only 12 of 36 modules — coverage breadth, not count, is the problem.
- Frontend: 1 vitest file, 3 tests (`act()` warning fixed in G2). `lint` is now **real**
  ESLint 9 (flat config) — 0 errors, 8 non-blocking warnings (6 `react-hooks/exhaustive-deps`,
  2 `react-refresh`) tracked as lint-debt; `typecheck` stays separate.
- Backend is `cargo fmt`-clean and `cargo clippy -p control-center -- -D warnings`-clean as of
  G2 (a deny-by-default `clippy::never_loop` compile error in `context_engine.rs` was fixed;
  15 dead-code items carry a `// unwired; reconciled in HARDPAN G5` marker).

---

## 5. Known-false claims in existing docs (STATUS deliberately disagrees)

Per the G0 exit gate, these are claims in the current tree that are **false against code**:

| Claim (source) | Reality |
|---|---|
| "Mission Control Dashboard (**the default tab**)" (`walkthrough.md:9`) | No `MissionControl` anywhere in `control-center/src` (grep empty). **Default tab is `Chat`** (`state/appStore.ts:18`, `App.tsx:75`). |
| `TerminalDrawer` 🟢 PASS (`QA_REPORT.md:29,37`) | Does not exist in the tree at all. |
| "Visual Org Chart renders in `ProjectDashboard.tsx`" 🟢 PASS (`QA_REPORT.md:50`) | Neither `VisualOrgChart` nor `ProjectDashboard` exists in `src` (only un-wired copies under `tools/legacy-repair/`). |
| "autonomous QA Tribunal" shipped (`SPRINT_LOG.md:41`, `QA_REPORT.md:76`) | No such component or tab; grep empty. |
| "Created `TerminalDrawer.tsx`" / "Constructed `VisualOrgChart.tsx`" (`SPRINT_LOG.md:21,26`) | Not in `src`. |
| `AUDIT_REAL_VS_FAKE.md` "43 backend tests" | Wrong — **44** measured on this host. (README's "44/44" is the correct one.) |
| `release-check.sh` is the release gate | Broken: hardcodes `CARGO_TARGET_DIR=/Volumes/Untitled/…` (`:12`) and calls a `./package.sh` that **doesn't exist** (`:22`). |

**Documentation kill-list** (executed in G6):

| Doc | Class | Action |
|---|---|---|
| `README.md`, `control-center/README.md` | truth | keep (reconcile test count) |
| `docs/AUDIT_REAL_VS_FAKE.md` | truth | keep; strip leaked `</content></invoke>` at **lines 123–124** |
| `docs/COMMAND_MATRIX.md` | truth | keep/expand (superseded by COMMAND_MAP.md) |
| `docs/DESIGN_SYSTEM.md`, `docs/SCHEMA.md`, `docs/FRONTEND_ARCHITECTURE.md`, `tools/legacy-repair/README.md` | truth | keep (FRONTEND_ARCHITECTURE: fix component names) |
| `docs/CURRENT_STATE.md`, `docs/KNOWN_BROKEN_FLOWS.md`, `docs/P0_*.md` (3), `docs/PHASE_3_*.md` (2) | stale | archive under `docs/archive/` with historical header |
| `walkthrough.md`, `docs/QA_REPORT.md`, `docs/SPRINT_LOG.md` | **fiction** | delete or fully rewrite |

**Real UI surface:** 10 tabs in sidebar order (`components/layout/Sidebar.tsx:4`) — Chat
(default), Kanban, Backlog, Agents, Memory, Context, Models, Runtime, Audit, Settings. Router
is a `switch(activeTab)` in `App.tsx:63–77` (no react-router). Pages: `ChatPage`,
`KanbanPage`, `BacklogPage`, `AgentsPage`, `ModelsPage`, and `OtherPages.tsx` (Memory /
Context / Runtime / Audit / Settings).

---

## 6. Receipts index ([`control-center/receipts/`](control-center/receipts/))

| Receipt | Class | What it proves |
|---|---|---|
| `frontend-baseline.check-receipt.json` | `cameleer.check-receipt/v1` | Frontend typecheck/vitest/p0 pass on the macOS target |
| _(G2)_ CI check-receipts | `check-receipt/v1` | Green frontend + backend jobs on GitHub Actions |
| _(G4)_ e2e receipt | `e2e-receipt/v1` | Full agent loop vs live camelid + persistence across restart |

---

## 7. HARDPAN phase status

| Phase | State |
|---|---|
| **G0** Ground truth (STATUS + COMMAND_MAP) | ✅ this file + COMMAND_MAP.md |
| **G1** Repo coherence / hard reset | ⏳ next |
| **G2** Portable release gate + CI | 🟡 all gate steps green locally (workspace-guard, typecheck, eslint, vitest, p0, `cargo fmt --check`, `cargo clippy -D warnings`, 44 tests, `package.sh` builds `.app`+`.dmg`). **`.github/workflows/ci.yml` is written and ready but NOT yet pushed** — the `gh` token lacks the `workflow` OAuth scope. Unblock with `gh auth refresh -s workflow` (or add the file via GitHub web), then the green-CI sub-gate completes. |
| **G3** Safety-critical test backfill | ⏳ |
| **G4** e2e receipt vs live inference | ⏳ |
| **G5** Backend↔frontend reconciliation | ⏳ |
| **G6** Doc truth pass + RC | ⏳ (tag needs explicit sign-off) |

_Last updated: HARDPAN G0._
